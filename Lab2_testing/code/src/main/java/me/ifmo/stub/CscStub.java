package me.ifmo.stub;

public class CscStub extends AbstractTableStub {

    public CscStub() {
        put(-Math.PI / 4, -Math.sqrt(2));
        put(-Math.PI / 3, -2.0 / Math.sqrt(3));
        put(-Math.PI / 6, -2.0);
    }
}
